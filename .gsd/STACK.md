# Technology Stack

> Auto-generated by /map on 2026-02-16

## Runtime

| Technology | Version | Purpose |
|------------|---------|---------|
| Python | 3.13 | Core runtime |
| PostgreSQL | — | Primary database (Supabase-hosted) |
| PostGIS | — | Geospatial extension for location data |

## Dependencies

### Production
| Package | Version | Purpose |
|---------|---------|---------|
| fastapi | ≥0.109.0 | Web framework (async) |
| uvicorn[standard] | ≥0.27.0 | ASGI server |
| sqlalchemy[asyncio] | ≥2.0.0 | Async ORM |
| asyncpg | ≥0.29.0 | PostgreSQL async driver |
| geoalchemy2 | ≥0.14.0 | PostGIS integration for SQLAlchemy |
| psycopg2-binary | ≥2.9.0 | PostgreSQL sync driver (for init scripts) |
| pydantic | ≥2.5.0 | Data validation & serialization |
| pydantic-settings | ≥2.1.0 | Environment-based configuration |
| python-dotenv | ≥1.0.0 | .env file loading |
| python-jose[cryptography] | ≥3.3.0 | JWT token creation & validation |
| httpx | ≥0.26.0 | Async HTTP client (SMS/Email APIs) |
| python-multipart | ≥0.0.6 | Form data parsing |
| aiosmtplib | ≥3.0.0 | Async SMTP (not yet used — email uses sync smtplib) |

### Development
| Package | Version | Purpose |
|---------|---------|---------|
| — | — | No dev dependencies specified |

## Infrastructure

| Service | Provider | Purpose |
|---------|----------|---------|
| Database | Supabase PostgreSQL | Primary data store with PostGIS |
| SMS (dev) | Console | OTP printing to stdout |
| SMS (prod) | MSG91 / Twilio | OTP delivery via SMS API |
| Email (dev) | Console | OTP printing to stdout |
| Email (prod) | SMTP (Gmail) | OTP delivery via email |

## Configuration

| Variable | Purpose | Location |
|----------|---------|----------|
| `DATABASE_URL` | PostgreSQL connection string | `.env` |
| `JWT_SECRET_KEY` | JWT signing key | `.env` |
| `JWT_ALGORITHM` | JWT algorithm (HS256) | `.env` |
| `ACCESS_TOKEN_EXPIRE_MINUTES` | Token TTL (30 min) | `.env` |
| `OTP_LENGTH` | OTP digit count (6) | `.env` |
| `OTP_EXPIRE_MINUTES` | OTP TTL (5 min) | `.env` |
| `OTP_MAX_ATTEMPTS` | Max verify attempts (3) | `.env` |
| `OTP_RESEND_COOLDOWN_SECONDS` | Resend cooldown (60s) | `.env` |
| `OTP_RATE_LIMIT_PER_HOUR` | Max OTPs per hour (5) | `.env` |
| `COLLEGE_EMAIL_PATTERN` | Regex for valid emails | `.env` |
| `SMS_PROVIDER` | SMS provider selection | `.env` |
| `SMS_API_KEY` | SMS API key | `.env` |
| `EMAIL_PROVIDER` | Email provider selection | `.env` |
| `SMTP_HOST` / `SMTP_PORT` | SMTP server config | `.env` |
| `SMTP_USER` / `SMTP_PASSWORD` | SMTP credentials | `.env` |

## Project Structure

```
backend/
├── .env / .env.example     # Environment configuration
├── requirements.txt        # Python dependencies
├── seed_data.py            # Database seeding script
├── venv/                   # Virtual environment
└── app/
    ├── main.py             # FastAPI application entry point
    ├── init_db.py          # Database table creation script
    ├── core/
    │   ├── config.py       # Settings (pydantic-settings)
    │   ├── deps.py         # Dependency injection (auth, DB)
    │   └── security.py     # JWT + OTP utilities
    ├── db/
    │   ├── base.py         # SQLAlchemy DeclarativeBase
    │   ├── enums.py        # Shared enums
    │   ├── session.py      # Async engine + session factory
    │   └── models/         # 13 SQLAlchemy models
    ├── routers/
    │   ├── auth.py         # OTP registration + login (7 endpoints)
    │   ├── users.py        # Profile management (2 endpoints)
    │   ├── vehicles.py     # Vehicle CRUD (3 endpoints)
    │   └── rides.py        # Ride management (3 endpoints)
    ├── schemas/            # Pydantic v2 request/response models
    └── services/
        ├── otp_service.py  # OTP lifecycle + rate limiting
        ├── sms_service.py  # SMS provider abstraction
        └── email_service.py # Email provider abstraction
```
